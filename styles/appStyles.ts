import { StyleSheet } from 'react-native'

const appStyles = StyleSheet.create({
    root: {
        alignItems: 'center',
        alignSelf: 'center'
    },

    greeting: {
        color: '#999',
        fontWeight: 'bold'
    }
});

export default appStyles

